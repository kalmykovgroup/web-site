# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_NAME=web_site
DB_USER=your_username
DB_PASSWORD=your_secure_password

# ASP.NET Core Environment
ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://+:8080

# Connection String (alternative to individual DB variables)
# ConnectionStrings__DefaultConnection=Host=postgres;Port=5432;Database=web_site;Username=username;Password=password

# Domain Configuration (for SSL/Nginx)
DOMAIN=localhost
# В production замените на ваш реальный домен:
# DOMAIN=yourdomain.com